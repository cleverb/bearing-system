# Escalation set

Tickets (or synthetic cases) whose correct answer is known in advance:
escalate, or proceed. Scores ESCALATE recall and false-escalation rate.

Populate `observed` after a human or a scored agent run. Empty or
unscored rows make `bearing verify` warn rather than pass the pillar
silently.
