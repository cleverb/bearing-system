# Gold Set

Known decisions, known Anchors — already well-annotated code with a real
@see ADR link. Tests recall: can the pipeline rediscover a decision that's
already documented?

Populate this with a small, hand-picked sample before trusting the
pipeline on the real backlog. Report precision/recall here before any new
extractor version or model tier goes live.
