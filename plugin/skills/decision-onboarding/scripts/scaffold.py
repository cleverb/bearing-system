#!/usr/bin/env python3
"""
Step 1: pure structure, no judgment calls.

Creates docs/decisions/ (README.md, index.json, shadow/ with its own
README.md and empty candidates.jsonl / rejected.jsonl), and .agents/
(AGENTS.md stub, decision-recovery and decision-interview Skills
installed but not yet run).

TODO: implement directory/file creation
TODO: pull AGENTS.md stub content from the BEARING template rather than
      hardcoding it here, so updates to the template propagate
"""

def main():
    raise NotImplementedError("See ../SKILL.md, Step 1.")

if __name__ == "__main__":
    main()
