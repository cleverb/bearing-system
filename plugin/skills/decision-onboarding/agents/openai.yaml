# DO NOT EDIT. Generated from plugin/skills/decision-onboarding/SKILL.md by bearing 0.1.0. Run `bearing render` to update; edits here are overwritten and reported as drift by `bearing render --check`.

interface:
  display_name: "Decision Onboarding"
  short_description: "Help a repository try BEARING, establish a small amount of useful decision context, and evaluate whether it is worth continuing"
policy:
  allow_implicit_invocation: false
  # Onboarding tags a baseline commit and creates a branch. That is a
  # deliberate, once-per-repository act, not something to infer from a prompt.
