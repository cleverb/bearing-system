# Agent-executed interview procedure

There is no `update-disclosure-index.py`. After a candidate is promoted
to an authored record, regenerate the index with `bearing index`.

Transcript path: `bearing transcripts` (fails if retention is `none`).
Candidate schema: `bearing schema candidate`. Ledger: `bearing ledger`.
